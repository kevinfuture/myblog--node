<% include header.ejs %>
<div>
    <% if (user) { %>
    <%='开园时间：'+user.baseObj[0].Date+'密码：'+user.password%>
</div>
<div class="upper-page clearfix">
    <!--这里是更换头像的页面-->
    <div class="form-horizontal base-msg l b" method="post">
        <div class="row">
            <div class="span8">
                <h3 class="titleBar">上传头像</h3>
                <div class="coninner">
                    <form name="avatarForm" enctype="multipart/form-data" action="/changephoto"  target="hideUpload" class="form-horizontal fn-mt20" method="post">
                        <div class="control-group">
                            <div class="controls"><img class="previewImg" src="<% if(user.headPic){ %><%= user.headPic %><% }else{ %>/images/nopic.jpg<% } %>"></div>
                        </div>
                        <div class="control-group">
                            <label for="J-headPic" class="control-label">URL：</label>
                            <div class="controls">
                                <input type="file" id="J-headPic" name="headPic">
                                <div class="remind"><i class="icon-exclamation-sign"></i>上传200K以内180x180高清无码照，接受gif|jpg|png格式。</div>
                            </div>
                        </div>
                        <div class="form-actions">
                            <input id="J-save-avatar" class="btn btn-large btn-primary" type="submit" value="保 存">
                        </div>
                    </form>
                </div>
            </div>

        </div>
        <iframe name="hideUpload" src="" class="hide"></iframe>
    </div>

    <% include rightslider.ejs %>
</div>
<% } %>
<!--没有用到，alert的显示，很漂亮-->
<script type="text/javascript">
    var uploadNotice;
    D.use(['jquery', 'alertify'], function($, alertify){
//        $(function(){
        uploadNotice = function(rec){
            alertify.alert(rec.data);
            if(rec.success){
                $('.previewImg, #J-user-infor-head').attr('src', rec.img +'?'+ new Date().getTime());
            }
        }
//        });
    });
</script>
<% include footer.ejs %>
